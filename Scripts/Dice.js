function Rolld4(amount){
	var count = 0;
	for (i = amount; i--; i >0) {
		count += parseInt(Math.random() * 4 + 1);
	}
	return count;
}

function Rolld6(amount){
	var count = 0;
	for (i = amount; i--; i >0) {
		count += parseInt(Math.random() * 6 + 1);
	}
	return count;
}

function Rolld8(amount){
	var count = 0;
	for (i = amount; i--; i >0) {
		count += parseInt(Math.random() * 8 + 1);
	}
	return count;
}

function Rolld10(amount){
	var count = 0;
	for (i = amount; i--; i >0) {
		count += parseInt(Math.random() * 10 + 1);
	}
	return count;
}

function Rolld12(amount){
	var count = 0;
	for (i = amount; i--; i >0) {
		count += parseInt(Math.random() * 12 + 1);
	}
	return count;
}

function Rolld20(amount){
	var count = 0;
	for (i = amount; i--; i >0) {
		count += parseInt(Math.random() * 20 + 1);
	}
	return count;
}

function Rolld100(amount){
	var count = 0;
	for (i = amount; i--; i >0) {
		count += parseInt(Math.random() * 100 + 1);
	}
	return count;
}